Anyone can (and must) contribute to [PAX<sup>GLOBAL</sup>](https://pax.global) 🕊️ Let's make this the repo with most contributors. 



```rust
const PAX: bool = true;

fn main() {
    let mut present_problems: Vec<_> = Vec::new();

    if present_problems.is_empty() {
        rest();
        identify_problem();
    } else {
        for _problem in &present_problems {
            fix_problem();
        }
    }

    while PAX {
        println!("PAX IS {PAX}")
    }
}
```

If you don't know how to open a PR, ask your programmer friend. If don't have one, make one.

